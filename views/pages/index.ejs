<style>
  /* ULTRA PREMIUM HOME PAGE STYLES */
  .section-heading {
    font-size: clamp(2rem, 5vw, 3rem);
  }
  .hero-premium {
    position: relative;
    min-height: 92vh;
    display: flex;
    align-items: center;
    background: url('/images/luxury-hero.png') center/cover;
    padding: 7.5rem 0 5rem;
    overflow: hidden;
  }
  .hero-premium::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(-45deg, rgba(255, 255, 255, 0.05), rgba(74, 222, 128, 0.1), rgba(255, 255, 255, 0.05), rgba(207, 157, 64, 0.05));
    background-size: 400% 400%;
    animation: auroraFlow 15s ease infinite;
    z-index: 1;
    pointer-events: none;
  }
  .hero-premium::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(105deg, rgba(4, 14, 10, 0.58) 0%, rgba(7, 24, 16, 0.4) 46%, rgba(6, 18, 13, 0.18) 100%);
    z-index: 2;
    pointer-events: none;
  }
  @keyframes auroraFlow {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  @keyframes pulseGlow {
    from { transform: scale(1) translate(0, 0); }
    to { transform: scale(1.1) translate(5%, 5%); }
  }
  .hero-grid {
    position: relative;
    z-index: 10;
    display: grid;
    grid-template-columns: minmax(0, 1.15fr) minmax(260px, 0.85fr);
    gap: 2rem;
    align-items: end;
  }
  .hero-content {
    position: relative;
    z-index: 10;
    max-width: 780px;
    background: rgba(5, 20, 13, 0.44);
    backdrop-filter: blur(7px);
    border: 1px solid rgba(255, 255, 255, 0.18);
    box-shadow: 0 20px 55px rgba(0, 0, 0, 0.32);
    border-radius: 28px;
    padding: 2rem 2rem 2.2rem;
  }
  .hero-badge {
    display: inline-block;
    padding: 0.5rem 1.2rem;
    background: rgba(255,255,255,0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.2);
    color: #f4c97d;
    border-radius: 30px;
    font-size: 0.9rem;
    font-weight: 600;
    margin-bottom: 2rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    animation: fadeUp 1s ease-out forwards;
  }
  .hero-title {
    font-size: clamp(3rem, 7vw, 5.5rem);
    font-family: var(--font-serif);
    color: #ffffff !important;
    line-height: 1.05;
    margin-bottom: 2rem;
    font-weight: 700;
    text-shadow: 0 10px 30px rgba(0, 0, 0, 0.52), 0 0 2px rgba(255, 255, 255, 0.45);
    opacity: 0;
    transform: translateY(30px);
    animation: fadeUp 1s ease-out 0.2s forwards;
    position: relative;
    z-index: 10;
  }
  .hero-subtitle {
    font-size: 1.2rem;
    color: #ffffff !important;
    line-height: 1.75;
    margin-bottom: 2.2rem;
    font-weight: 600;
    text-shadow: 0 3px 12px rgba(0, 0, 0, 0.5), 0 0 1px rgba(255, 255, 255, 0.35);
    opacity: 0;
    transform: translateY(30px);
    animation: fadeUp 1s ease-out 0.4s forwards;
    max-width: 600px;
  }
  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
    opacity: 0;
    transform: translateY(30px);
    animation: fadeUp 1s ease-out 0.6s forwards;
  }
  .hero-buttons .btn {
    position: relative;
    z-index: 12;
    pointer-events: auto;
    color: #ffffff !important;
    font-weight: 700;
    letter-spacing: 0.2px;
    min-width: 220px;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.35);
  }
  .hero-btn-primary {
    background: linear-gradient(135deg, #1f7a52 0%, #0f5b3a 100%);
    border-color: transparent;
    box-shadow: 0 12px 30px rgba(16, 125, 78, 0.45) !important;
  }
  .hero-btn-outline {
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.7);
  }
  .hero-meta {
    display: flex;
    gap: 1.1rem;
    flex-wrap: wrap;
    margin-top: 0.4rem;
  }
  .hero-chip {
    padding: 0.45rem 0.95rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    font-size: 0.85rem;
    color: #eaf8f1;
    font-weight: 500;
  }
  .hero-sidecard {
    background: rgba(255, 255, 255, 0.92);
    border-radius: 22px;
    padding: 1.4rem;
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.6);
    max-width: 360px;
    justify-self: end;
  }
  .hero-sidecard h3 {
    font-family: var(--font-serif);
    color: var(--primary-dark);
    margin-bottom: 0.85rem;
    font-size: 1.3rem;
  }
  .hero-sidecard p {
    color: #3b4c45;
    margin-bottom: 1rem;
    font-size: 0.96rem;
    line-height: 1.6;
  }
  .hero-sidecard ul {
    display: grid;
    gap: 0.6rem;
  }
  .hero-sidecard li {
    color: var(--primary-dark);
    font-size: 0.9rem;
    font-weight: 600;
  }

  @keyframes fadeUp {
    to { opacity: 1; transform: translateY(0); }
  }

  /* Trust Bar */
  .trust-bar {
    background: white;
    padding: 2.5rem;
    border-radius: 20px;
    box-shadow: 0 25px 50px rgba(16,42,29,0.08);
    position: relative;
    z-index: 10;
    margin: -4rem auto 4rem;
    width: 90%;
    max-width: 1200px;
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 2rem;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: transform 0.3s;
  }
  .trust-item:hover { transform: translateY(-5px); }
  .trust-icon { font-size: 2.2rem; background: var(--bg-light); padding: 1rem; border-radius: 50%; color: var(--primary); }
  .trust-text h4 { margin: 0 0 0.2rem; font-size: 1.1rem; color: var(--text-dark); }
  .trust-text p { margin: 0; font-size: 0.85rem; color: var(--text-light); }

  /* Premium About Section */
  .home-about {
    padding: 6rem 0;
    background: #fdfdfd;
  }
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .about-img-wrapper {
    position: relative;
    border-radius: 30px;
    overflow: hidden;
    box-shadow: 0 30px 60px rgba(0,0,0,0.1);
  }
  .about-img-wrapper img {
    width: 100%;
    height: 600px;
    object-fit: cover;
    transition: transform 0.7s;
  }
  .about-img-wrapper:hover img {
    transform: scale(1.05);
  }
  .floating-badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    background: var(--primary);
    color: white;
    padding: 2.5rem;
    border-radius: 50%;
    width: 150px;
    height: 150px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    border: 10px solid #fdfdfd;
    box-shadow: var(--shadow-md);
  }

  /* Featured Services Carousel/Grid */
  .featured-services {
    padding: 8rem 0;
    background: linear-gradient(135deg, #f0f7f4 0%, #e2ece9 100%);
  }
  .premium-card {
    background: white;
    border-radius: 25px;
    overflow: hidden;
    position: relative;
    transition: all 0.4s ease;
    box-shadow: 0 15px 35px rgba(0,0,0,0.05);
    border: none;
  }
  .premium-card:hover {
    transform: translateY(-15px);
    box-shadow: 0 40px 60px rgba(16,42,29,0.1);
  }
  .card-img-bx {
    height: 250px;
    overflow: hidden;
    position: relative;
  }
  .card-img-bx img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  .premium-card:hover .card-img-bx img {
    transform: scale(1.1);
  }
  .card-content-bx {
    padding: 2.5rem 2rem;
    position: relative;
  }
  .card-icon-float {
    position: absolute;
    top: -30px;
    right: 30px;
    background: var(--accent);
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.5rem;
    box-shadow: 0 10px 20px rgba(245, 158, 11, 0.4);
    color: white;
  }

  /* Testimonials */
  .testimonials {
    padding: 8rem 0;
    text-align: center;
    background: white;
  }
  .testimonial-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; margin-top: 4rem; }
  .testimonial-box {
    padding: 3rem 2rem;
    background: #fdfdfd;
    border: 1px solid #f0f0f0;
    border-radius: 20px;
    transition: box-shadow 0.3s;
    text-align: left;
    position: relative;
  }
  .testimonial-box:hover { box-shadow: 0 20px 40px rgba(0,0,0,0.04); }
  .quote-mark { font-size: 4rem; color: var(--primary-light); opacity: 0.2; position: absolute; top: 10px; right: 20px; font-family: serif; }

  @media (max-width: 900px) {
    .about-grid { grid-template-columns: 1fr; gap: 3rem; }
    .about-img-wrapper img { height: 400px; }
    .floating-badge { display: none; }
    .hero-premium { 
      height: auto; 
      min-height: 100vh; 
      padding: 120px 0 60px;
      background-attachment: scroll; /* Better for mobile performance */
    }
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 1.2rem;
    }
    .hero-sidecard {
      max-width: 100%;
      justify-self: start;
      background: rgba(255, 255, 255, 0.95);
    }
    .hero-title { font-size: 2.8rem; word-break: break-word; }
    .hero-badge { margin-bottom: 1.5rem; }
    .hero-subtitle { font-size: 1.1rem; margin-bottom: 2rem; }
    .hero-content { padding: 1.3rem; }
  }
  @media (max-width: 480px) {
    .hero-title { font-size: 2.2rem; }
    .hero-buttons { flex-direction: column; width: 100%; }
    .hero-buttons .btn { width: 100%; text-align: center; justify-content: center; min-width: 0; }
  }

  @media (max-width: 600px) {
    .hero-title { font-size: 2.5rem; }
    .trust-bar { padding: 1.5rem; margin-top: -2rem; }
    .section-title h2, .about-text h2, .featured-services h2 { font-size: 2.2rem !important; }
  }
</style>

<!-- ULTRA PREMIUM HERO SECTION -->
<section class="hero-premium">
  <div class="container">
    <div class="hero-grid">
      <div class="hero-content">
        <span class="hero-badge">Welcome to Jeevadhara</span>
        <h1 class="hero-title">Awaken Your<br>Inner Harmony</h1>
        <p class="hero-subtitle">
          Embark on a transformative journey of holistic wellness. We blend ancient Naturopathic wisdom with modern luxury to rejuvenate your mind, body, and spirit.
        </p>
        <div class="hero-buttons">
          <a href="/services" class="btn btn-primary hero-btn-primary">Discover Therapies</a>
          <a href="/about" class="btn btn-outline hero-btn-outline">Our Journey</a>
        </div>
        <div class="hero-meta">
          <span class="hero-chip">100% Natural Protocols</span>
          <span class="hero-chip">Expert Healing Team</span>
          <span class="hero-chip">Personalized Care Plans</span>
        </div>
      </div>
      <aside class="hero-sidecard">
        <h3>Your Wellness Blueprint</h3>
        <p>Choose a complete path designed for energy, pain relief, and deep relaxation.</p>
        <ul>
          <li>+ Body & Mind Rebalancing</li>
          <li>+ Naturopathy & Therapeutic Yoga</li>
          <li>+ Guided Nutrition and Detox</li>
        </ul>
      </aside>
    </div>
  </div>
</section>

<!-- TRUST BAR -->
<div class="trust-bar">
  <div class="trust-item">
    <div class="trust-icon">🌿</div>
    <div class="trust-text">
      <h4>100% Natural</h4>
      <p>Holistic & Organic Care</p>
    </div>
  </div>
  <div class="trust-item">
    <div class="trust-icon">👨‍⚕️</div>
    <div class="trust-text">
      <h4>Expert Healers</h4>
      <p>Certified Practitioners</p>
    </div>
  </div>
  <div class="trust-item">
    <div class="trust-icon">✨</div>
    <div class="trust-text">
      <h4>Personalised</h4>
      <p>Tailored Treatments</p>
    </div>
  </div>
</div>

<!-- ABOUT EXCERPT -->
<section class="home-about">
  <div class="container about-grid">
    <div class="about-img-wrapper">
      <img src="https://images.unsplash.com/photo-1600334129128-685c5582fd35?auto=format&fit=crop&q=80&w=800" alt="Spa and Wellness">
      <div class="floating-badge" style="display: none;">
        <!-- Badge Removed -->
      </div>
    </div>
    <div class="about-text">
      <span style="color: var(--accent); font-weight: 600; letter-spacing: 2px; text-transform: uppercase; font-size: 0.9rem;">The Flow of Life</span>
      <h2 style="font-family: var(--font-serif); margin: 1rem 0 2rem; color: var(--primary-dark); line-height: 1.2;" class="section-heading">Sanctuary for the Soul,<br>Healing for the Body</h2>
      <p style="font-size: 1.1rem; color: var(--text-light); margin-bottom: 1.5rem; line-height: 1.8;">
        At Jeevadhara, we believe true wellness comes from aligning yourself with nature. We provide deeply immersive, completely drug-free therapies drawn from the world's oldest healing traditions.
      </p>
      <p style="font-size: 1.1rem; color: var(--text-light); margin-bottom: 2.5rem; line-height: 1.8;">
        Whether you are seeking relief from chronic pain or just a momentary escape into profound relaxation, our sanctuary is designed exclusively for your rejuvenation.
      </p>
      <a href="/about" class="btn btn-outline" style="border-color: var(--primary); color: var(--primary); border-radius: 30px; padding: 0.8rem 2rem;">Read Our Full Story &rarr;</a>
    </div>
  </div>
</section>

<!-- PREMIUM FEATURED SERVICES -->
<section class="featured-services">
  <div class="container">
    <div style="display: flex; justify-content: space-between; align-items: flex-end; margin-bottom: 4rem; flex-wrap: wrap; gap: 2rem;">
      <div style="max-width: 600px;">
        <span style="color: var(--primary); font-weight: 600; letter-spacing: 2px; text-transform: uppercase; font-size: 0.9rem;">Curated Experiences</span>
        <h2 style="font-family: var(--font-serif); color: var(--primary-dark); margin-top: 1rem;" class="section-heading">Oasis of Therapies</h2>
      </div>
      <a href="/services" class="btn btn-primary" style="background: white; color: var(--primary); border-radius: 30px; box-shadow: 0 10px 20px rgba(0,0,0,0.05);">View All Services</a>
    </div>

    <div class="services-grid">
      <!-- Card 1 -->
      <div class="premium-card">
        <div class="card-img-bx">
          <img src="https://images.unsplash.com/photo-1544161515-4ab6ce6db874?auto=format&fit=crop&q=80&w=800" alt="Massage">
        </div>
        <div class="card-content-bx">
          <div class="card-icon-float">🌿</div>
          <span style="color: var(--text-light); font-size: 0.9rem; font-weight: 600;">BODYWORK</span>
          <h3 style="font-size: 1.5rem; margin: 0.5rem 0 1rem; color: var(--text-dark);">Aromatherapy Massage</h3>
          <p style="color: var(--text-light); font-size: 0.95rem; line-height: 1.6; margin-bottom: 1.5rem;">A deeply relaxing full-body massage using highly potent essential oils designed to melt away physical tension.</p>
          <a href="/login" style="color: var(--primary); font-weight: bold; text-decoration: none; display: flex; align-items: center; gap: 0.5rem;">Book Session &rarr;</a>
        </div>
      </div>

      <!-- Card 2 -->
      <div class="premium-card">
        <div class="card-img-bx">
          <img src="https://images.unsplash.com/photo-1512621776951-a57141f2eefd?auto=format&fit=crop&q=80&w=800" alt="Diet">
        </div>
        <div class="card-content-bx">
          <div class="card-icon-float">🥗</div>
          <span style="color: var(--text-light); font-size: 0.9rem; font-weight: 600;">NUTRITION</span>
          <h3 style="font-size: 1.5rem; margin: 0.5rem 0 1rem; color: var(--text-dark);">Detox Diet Plans</h3>
          <p style="color: var(--text-light); font-size: 0.95rem; line-height: 1.6; margin-bottom: 1.5rem;">Revitalize your internal ecosystem with an expertly crafted, 100% natural, holistic detox diet plan.</p>
          <a href="/login" style="color: var(--primary); font-weight: bold; text-decoration: none; display: flex; align-items: center; gap: 0.5rem;">Book Session &rarr;</a>
        </div>
      </div>

      <!-- Card 3 -->
      <div class="premium-card">
        <div class="card-img-bx">
          <img src="https://images.unsplash.com/photo-1599901860904-17e6ed7083a0?auto=format&fit=crop&q=80&w=800" alt="Yoga">
        </div>
        <div class="card-content-bx">
          <div class="card-icon-float">🧘‍♀️</div>
          <span style="color: var(--text-light); font-size: 0.9rem; font-weight: 600;">MINDFULNESS</span>
          <h3 style="font-size: 1.5rem; margin: 0.5rem 0 1rem; color: var(--text-dark);">Vinyasa Flow Yoga</h3>
          <p style="color: var(--text-light); font-size: 0.95rem; line-height: 1.6; margin-bottom: 1.5rem;">Awaken your dormant energy through a guided, breathable sequence of flowing yogic postures.</p>
          <a href="/login" style="color: var(--primary); font-weight: bold; text-decoration: none; display: flex; align-items: center; gap: 0.5rem;">Book Session &rarr;</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Testimonials Section Removed -->